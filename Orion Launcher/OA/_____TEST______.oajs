//#include Autoload.oajs

function TEST()
{
	while (!Player.Dead())
	{
		var target = Orion.FindType('any', 'any', ground, 'near|mobile|ignorefriends|ignoreself|inlos', '12', 'gray|criminal|orange|red');
	    if (target && target.length > 0)
	    {
	        Orion.TargetSystemSerial(target);
	        Orion.Attack(target);
	    }
	    Orion.Wait(100);
    }
}

function HealPet()
{
	var derp = '0x0076DF69';
	while (!Player.Dead()) {
		Orion.UseType('0x0E21', any);
		Orion.WaitForTarget(1000);
		Orion.TargetObject('0x0076DF69');
		Orion.Wait(2100);
	}
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////
function GetProperties()
{
	var props = Orion.FindObject('0x423892B0').Properties();
	var result = '';
	
	Orion.Print('PROPS: ' + props + ' ...END PROPS');
	
	for (var i = props.length - 3; i < props.length; i++)
	{
		if (parseInt(props[i]))
			result += props[i];
		Orion.Print(result);
	}
	
	return result;
}














