
function TEST()
{
	var corpseList = Orion.FindType('0x2006', any, ground, 'ignoreself', 10);

	for (var i=0; i < corpseList.length; i++) {
		var itemList = Orion.FindList('Autoloot', corpseList[i]);
		// for (var j=0; j < itemList.length; j++) {
		// 	Orion.Print(Orion.FindObject(itemList[j]).Properties());
		// }
		//Orion.Print(89, Orion.FindObject(mobList[i]).Properties() + '\n');
	}
}

function HealPet()
{
	var herp = '0x001DC91C';
	var derp = '0x0076DF69';
	while (!Player.Dead()) {
		Orion.UseType('0x0E21', any);
		Orion.WaitForTarget(1000);
		Orion.TargetObject(derp);
		Orion.Wait(2100);
	}
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////
function GetProperties()
{
	Orion.WaitForAddObject('Target');;
	var props = Orion.FindObject('Target').Properties();
	var result = '';
	
	Orion.Print('_____ Properties _____ \n' + props + '\n ________________');
	
	for (var i = props.length - 3; i < props.length; i++)
	{
		if (parseInt(props[i]))
			result += props[i];
		Orion.Print(result);
	}
	
	return result;
}














